import spacing, { screenWidth, screenHeight } from "@/styles/spacing";

export const THROTTLE_DELAY = 1000;

export const HORIZONTAL_PADDING = spacing.md * 2;
export const CHART_WIDTH = screenWidth - HORIZONTAL_PADDING;
export const CHART_HEIGHT = screenHeight / 4.5;
